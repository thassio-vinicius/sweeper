/// Routes owned by the game feature (referenced by the app shell router).
abstract final class GameNavigation {
  static const home = '/';
  static const gameOver = '/game-over';
  static const login = '/login';
}
